import React from 'react';

export default function DifficultySettings() {
    return (
        <div>
            <span>Difficulty: </span>
            <select>
                <option>Easy</option>
                <option>Hard</option>
            </select>
        </div>
    );
}